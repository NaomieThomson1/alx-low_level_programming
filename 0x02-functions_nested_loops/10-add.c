#include "main.h"

/**
 * add - check main
 * @a: a is an integer
 * @b: b is an integer
 * Description: function that adds two integers and return the result
 * Return: results of adding a and b
 */

int add(int a, int b)
{
	return (a + b);
}
