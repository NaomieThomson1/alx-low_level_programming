#include "main.h"

/**
 * _abs - check main
 * @b: an intefer input
 * Dscription: this function returns absolute value of a numberr
 * Return: absolute value of number b
 */

int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (b * -1);
}
